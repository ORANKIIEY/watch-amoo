# Server
PORT=4000
FRONTEND_ORIGIN=http://localhost:3000

# SQLite (relative to backend/prisma/schema.prisma)
DATABASE_URL="file:../data/watchamoo.db"

# Email — pick ONE option
# Option A: Resend
RESEND_API_KEY=re_xxxxxxxx
EMAIL_FROM=watchamoo <onboarding@resend.dev>

# Option B: SMTP
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=you@gmail.com
# SMTP_PASS=your-app-password
# EMAIL_FROM=watchamoo <you@gmail.com>

# Option C: local only — print OTP to the backend console (never in production)
# EMAIL_DEV_LOG=true
